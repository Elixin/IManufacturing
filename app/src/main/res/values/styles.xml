<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!--Theme.AppCompat.Light.NoActionBar-->
        <!--Theme.AppCompat.Light.DarkActionBar-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="WebViewProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">20dip</item>
        <item name="android:maxHeight">20dip</item>
    </style>
    <style name="Mytabstyle" parent="Widget.Design.TabLayout">
        <item name="tabTextColor">#fff</item>
        <item name="tabSelectedTextColor">@color/black1</item>
        <item name="tabIndicatorHeight">0dp</item>
    </style>
    <style name="intotabstyle" parent="Widget.Design.TabLayout">
        <item name="tabTextColor">#fff</item>
        <item name="tabSelectedTextColor">@color/black1</item>
        <item name="tabBackground">@color/black3</item>
        <item name="tabIndicatorHeight">0dp</item>
    </style>
    <style name="TabLayoutTextStyle">
        <item name="android:textSize">20sp</item>
    </style>

</resources>
